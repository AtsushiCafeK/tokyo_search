# Region Persistence using Cookies Plan

The user wants to persist the selected region across sessions using cookies. While `localStorage` is currently used, we will add/switch to cookie-based persistence as requested.

## Proposed Changes

### Utilities

#### [NEW] [utils/cookies.ts](file:///Users/atsushishindo/Desktop/GovSearch/gov_hachiouji/src/utils/cookies.ts)
- Implement simple `setCookie` and `getCookie` functions using `document.cookie`.
- Ensure they are client-side safe.

### Components & Pages

#### [MODIFY] [page.tsx](file:///Users/atsushishindo/Desktop/GovSearch/gov_hachiouji/src/app/page.tsx)
- Import cookie utilities.
- Update `useEffect` (mount) to prioritize cookies (or fallback to `localStorage` for migration).
- Update `handleRegionSelect` to save the selected municipality ID to cookies.
- Update `handleToggleMetro` to save the toggle state to cookies as well.

#### [MODIFY] [roadmap/page.tsx](file:///Users/atsushishindo/Desktop/GovSearch/gov_hachiouji/src/app/roadmap/page.tsx)
- Move "地域の自動記憶機能（Cookie/LocalStorage）" from `IN_PROGRESS_TASKS` to `COMPLETED_TASKS`.
- Set progress to 100%.

## Verification Plan

### Manual Verification
- Open the site, select a different municipality (e.g., Shinjuku).
- Refresh the page.
- Verify the selected municipality remains "Shinjuku".
- Check the "Cookies" tab in browser DevTools to see `selected_municipality_id` is set.
- Run `npm run build` to ensure no build errors.
